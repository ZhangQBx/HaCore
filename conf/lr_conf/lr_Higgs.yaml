is_regression: False
dataset_name: "higgs"
data_path: "../../datasets/raw_data/Higgs.csv"
epochs: 100
batch_size_normal:
batch_size_lsh:
lr:
lr_gamma:
lr_step:
kmeans:
  k_per_clients:  # core-set size
num_hash_funs: # total number of hash functions
bucket_width: 9