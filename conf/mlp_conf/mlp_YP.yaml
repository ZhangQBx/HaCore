is_regression: True
dataset_name: "yp"
data_path: "../../datasets/raw_data/YearPredictionMSD.csv"
epochs: 100
batch_size_normal:
batch_size_lsh:
lr:
lr_gamma:
lr_step:
n_bottom_out: 32
n_top_out: 1
kmeans:
  k_per_clients:  # core-set size
num_hash_funs: # total number of hash functions
bucket_width: 9